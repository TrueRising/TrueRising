export const Badge = ({ text }: { text: string }) => (
  <span className="inline-flex rounded-full border border-borderSubtle bg-surfaceElevated px-3 py-1 text-xs text-textSecondary">{text}</span>
);
